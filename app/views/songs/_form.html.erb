<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_id %>
  <%= artist_select(@artist_id, @song.artist) %>
  <br />

  <%= f.submit %>
<% end %>
