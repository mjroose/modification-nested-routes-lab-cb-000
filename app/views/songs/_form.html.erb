<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.fields_for :artist do %>
    <%= artist_select(@artist_id, @song.artist) %>
  <% end %>
  <br />

  <%= f.submit %>
<% end %>
